to see result just run index.html from build folder

for development you need first to install webpack globally
`:~ npm i -g webpack`
and then run commands from repository's directory
`:~ npm i`
`:~ webpack`